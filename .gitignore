# Боевая база данных — НЕ хранится в репозитории.
# Живёт на сервере: /var/sk-pilot/db/pilot.db
db/pilot.db
db/pilot.db-shm
db/pilot.db-wal
db/*.db
db/*.db-shm
db/*.db-wal

# Загруженные пользователями файлы (фотографии, протоколы)
uploads/

# Ключ подписи сессий — секрет, генерируется на каждой машине свой
.secret_key

# Python
__pycache__/
*.pyc
